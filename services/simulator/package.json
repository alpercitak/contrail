{
  "name": "@contrail/simulator",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch src",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "@contrail/shared": "workspace:*",
    "@contrail/simulation": "workspace:*",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}

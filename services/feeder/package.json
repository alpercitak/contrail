{
  "name": "@contrail/feeder",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun --watch src",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "@contrail/feed-mock": "workspace:*",
    "@contrail/logger": "workspace:*",
    "@contrail/shared": "workspace:*",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "pino-pretty": "catalog:",
    "typescript": "catalog:"
  }
}

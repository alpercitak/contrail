{
  "name": "@contrail/gateway",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "@contrail/shared": "workspace:*",
    "@fastify/websocket": "^11.2.0",
    "fastify": "catalog:",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
